# AI 产品工厂｜前端技术适配声明

> 状态：产品负责人已于 2026-08-20 回复“按推荐”，确认采用现有 Next.js 应用内的真实代表页面方案。

## 1. 项目判断

- 产品类型：本地优先、单用户使用的通用 AI 数字产品生产控制台。
- 主要用户：第一版仅产品负责人本人。
- 主要终端：桌面 Web，核心流程兼顾 390px 移动 Web。
- 新项目或已有项目：已有 Next.js Web 项目，已经存在项目列表、创建项目、项目详情和运行控制台。
- 本阶段业务目标：把现有 WebUI 收缩为可直接操作的生产控制页，只保留当前步骤、AI 结果和真实确认入口。

## 2. 输入资料

- 产品 PRD：工厂级定义已由 `docs/01-产品定义.md` 等现有文档表达；首台小游戏 PRD 尚未提供。
- 三份原始手册：已完整读取，共 2659 行；完整性由 `docs/MANUALS.sha256` 校验。
- 后端手册：已读取《AI Agent 产品 Vibe Coding 通用技术栈手册》V2.1。
- 接口文档：当前由 TypeScript 类型、Route Handler 与现有实现共同表达，尚无独立 OpenAPI。
- 设计稿：无。
- 现有代码：有；Next.js 16.3.1、React 19、TypeScript strict、现有 CSS 语义变量、SQLite、SSE。

## 3. 技术选择

- 框架与版本：延续现有 Next.js 16.3.1 + React 19 + TypeScript，不重建平行前端。
- 页面产物：在现有 Web 应用中生成浏览器可访问、最终渲染为 HTML 的代表页面；不另建一套与真实应用脱节的 Mock 前端。
- 样式与组件策略：延续现有语义 CSS 变量和轻量组件；本阶段不引入新组件库。
- 数据请求与状态策略：服务端状态以 SQLite/API 为事实来源；只把页面标签等临时交互留在客户端。
- 流式通信策略：沿用现有 SSE 运行事件流；代表页面不伪造真实生产成功。
- 表单方案：本代表页面优先复用已有创建项目与启动批次入口，不新增复杂表单库。
- 测试方案：Lint、TypeScript、Vitest、生产构建和真实浏览器桌面/390px 检查；真实 DeepSeek 冒烟在配置 Key 后完成。
- 部署环境：当前本地运行；正式上线阶段严格进入《上线部署手册》的 veFaaS 流程。

## 4. Agent 状态

- 后端状态 → 前端统一状态映射：现有 `ready`、`running`、`waiting_approval`、`succeeded`、`failed`、`blocked` 分别表达等待开始、执行中、等待用户、已完成、失败和等待配置。
- 等待用户确认方式：由确定性生产控制器持久化闸门和创建下一工位；页面只展示当前结果和业务含义明确的确认按钮。
- 取消、重试、断线恢复方式：本阶段只展示真实可用能力；暂停、steer、继续和 abort 尚未完成时不提供误导按钮。
- 任务 ID 和恢复入口：生产批次 ID 保存在 URL；刷新后从 SQLite 重新读取状态和事件。

## 5. 页面与产物

- 核心页面：首页、产品页、新建页和运行确认页。
- 核心业务闭环：新建产品 → 开始分析 → 查看 AI 结果 → 确认产品理解 → 确认技术方案 → 进入开发计划。
- 产物类型和预览方式：本阶段展示生产档案、事件和闸门；后续统一支持文本、图片、视频、音频、文件和 JSON 产物。
- 响应式目标：390px、768px、1280px、1440px；无意横向滚动为零。

## 6. 偏离本手册的地方

- 默认后端基线偏离：工厂控制平面继续使用现有 Node.js/TypeScript 模块化单体，而不是新建 Python/FastAPI；原因是现有系统已稳定运行，手册明确要求已有项目延续合理架构。影响是当前没有 FastAPI/OpenAPI，但接口、校验、持久化和测试底线保持不变。
- Tailwind 使用方式偏离：现有界面以 CSS 语义变量和自建样式为主，仅保留 Tailwind 构建入口；本阶段延续现状，避免为形式统一重构。
- 真实模型冒烟：2026-08-21 已通过本地后端安全配置接入 `DEEPSEEK_API_KEY`，使用 `deepseek-v4-flash` 完成一次真实端到端验证；密钥未进入前端、仓库、数据库事件或日志。

## 7. 待确认事项

- 已确认：把“HTML 前端页面”实现为现有 Next.js 应用中的真实代表页面，复用真实数据和路由，不创建平行的静态 Mock 页面。
- 无待确认事项。

## 8. 本轮实现与验证

- 已删除页面中与当前操作无关的介绍、统计、宣传性路线和内部技术词；三份手册仍作为后端最高执行规范，不在公开页面分发原文。
- 已实现首页“去确认”、运行页真实确认按钮和两道规划闸门流转。
- 已补齐路由级加载状态和错误恢复状态；错误页面不把生产档案误显示为空，也不向用户展示堆栈。
- 已验证真实阻塞批次仍显示“等待配置”和 `DEEPSEEK_API_KEY` 缺失原因。
- 已验证真实 DeepSeek 批次 `9a58131d-a790-4ae8-8dfd-fad7a0f5534c` 成功完成：任务从创建、领取、Agent 流式输出到成功收尾均已持久化；共保存 2116 条事件和 3492 个结果字符，端到端约 108 秒。
- 已验证 390px 和 1440px 的首页、产品页、新建页和运行页均无横向溢出，Console 无应用自身错误。
- 当前事件映射尚未保存模型 Token 与费用，本轮只能如实记录模型、耗时、事件和结果规模；用量摘要仍属于里程碑 2 待完成项。
